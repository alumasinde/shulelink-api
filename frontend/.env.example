# Leave empty to resolve the API from the current platform/school hostname.
# For a fixed local platform API, use: http://admin.localhost:8000/api/v1
VITE_API_URL=

# Keep false for the existing local bearer-token development flow.
# Production must use true so browser sessions remain in Secure HttpOnly cookies.
VITE_AUTH_COOKIE_MODE=false
